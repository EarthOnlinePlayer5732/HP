# Current Goal State

Updated: 2026-07-04

Goal status: Phase 0-5 frozen val/test chain complete; a new dev iteration is open for HybridPatch improvements (P0 transport fix first). Not blocked.

The external goal objective file is older than the workspace state. Use this file, `analysis/active_iteration_log.md`, `analysis/candidate_registry.md`, and `anchorpatch/ANCHORPATCH_DESIGN_AND_FINDINGS.md` as the current authoritative state.

## Logging Layout

- Root prompt log: `anchorpatch/PROMPT_GENERALIZATION_LOG.md`.
- C30+ current prompt-log entry: `anchorpatch/PROMPT_GENERALIZATION_LOG_C30_PLUS_CURRENT.md`.
- C30+ compact prompt-log index: `anchorpatch/PROMPT_GENERALIZATION_LOG_C30_PLUS_INDEX.md`.
- C30+ detailed prompt-history archive router: `anchorpatch/PROMPT_GENERALIZATION_LOG_C30_PLUS.md`.
- Current before/after cursor: `analysis/active_iteration_log.md`.
- Candidate evidence index: `analysis/candidate_registry.md`.
- Latest active cursor archive: `analysis/active_iteration_log_C66_C69_IN_PROGRESS_ARCHIVE_20260626.md`.

Routine handoff should read the root prompt log, the C30+ current entry, this state file, the active cursor, the C30+ compact index, and the candidate registry. Open detailed archives only for exact historical traceback. Do not append active work to the root prompt log, C30+ current entry, legacy handoff pointer, compact index, or archives.

## Current Goal Track

Active track: `hybridpatch vs fullrewrite`.

Governance switch:

- C71 AP-pure strategy gate is closed without starting.
- The C30+ AP-pure line remains archived evidence only; its old guardrail ("do not use router/FR rows as AP-pure evidence") applies to that historical line.
- New canonical method id: `hybridpatch`. Aliases are forbidden.
- Frozen paths remain frozen: v1 `anchorpatch`, `anchorpatch_v2`, and `anchorpatch_v2_2` must not be modified for this track.

HybridPatch guardrail:

- Honesty gate must deterministically replay every `hybridpatch` path from raw responses before any hybrid number is reported.
- Bounded rewrite route share must be reported for every experiment. It has no hard cap, but it is a required identity-dilution disclosure.
- Prompt/protocol iteration is dev-only. After val freeze, any code or prompt fingerprint change sends the work back to dev.
- Failed steps carry the workspace forward unchanged and mark failure telemetry. Silent FR fallback is forbidden; v1 `full_rewrite_fallback` is not part of `hybridpatch`.
- `hybridpatch` may use one primary LLM call plus at most one JSON/schema-only repair call. Semantic/gate failures do not repair.

Current split artifact: `analysis/hybrid_split.json` (`dev=20`, `val=20`, `test=20`, `unused_reserve=20`), materialized from existing `dev20_20260625` and `holdout_reserve_20260625`.

Phase 1 no-API wiring status:

- Hybrid schema/index/prompt/executor/gate/test modules are present under `anchorpatch/`.
- `experiment_runner.py` has a top-level `hybridpatch` branch; failures keep context and record `bdpatch.hybrid` rather than falling back to FR.
- `verify_anchorpatch.py` can replay hybrid primary/repair raw responses through `_apply_hybrid`.
- `analyze.py` reports hybrid RS@K, CriticalFailure, route share, bounded rewrite share, repair/gate/no-op rates, and copied/generated bytes.
- No-API validation passed on 2026-07-03: `test_hybrid_executor.py`, existing `test_executor.py`, `splitters.py`, `py_compile`, split generation, and one bounded-rewrite runner dry-run.

Dev-mini status:

- Out dir: `anchorpatch/exp_20260703_hybridsmoke`.
- Samples: `foodmenu6`, `circuit2`, `musicsheet2`, `python7`, `mathlean2`, `fonteng3`, `screenplay6`, `docker6`, `protein1`, `edifact6`.
- Model/settings: `minimax-m3`, `10RT`, `--skip_distractor`, paired `hybridpatch` then `fullrewrite`.
- Raw rows: `hybridpatch=200`, `fullrewrite=200`; `verify_anchorpatch.py --dir anchorpatch/exp_20260703_hybridsmoke` PASS with `200` backward RS reproduced.
- CriticalFailure theta is frozen at `0.10` from dev-mini calibration. Calibration used `53` positive nonzero adjacent backward RS drops across both methods; p75 floor-index value was `0.052855...`, so the smallest candidate at or above p75 is `0.10`.
- Dev-mini metrics after replay: `hybridpatch` RS@10 `0.811`, `fullrewrite` RS@10 `0.728`; paired backward mean delta `+0.093` over `100` pairs; CriticalFailure@10 `hybridpatch=5/90`, `fullrewrite=6/90`.
- Hybrid telemetry: route share `bounded_rewrite=103/200 (51.5%)`, `local_patch=82/200 (41.0%)`, `bulk_patch=12/200 (6.0%)`, `dsl_rules=3/200 (1.5%)`; kept-context/gate failures `64/200`; forward no-effective-modification audit `8/100`.

Distractor probe status:

- Out dir: `anchorpatch/exp_20260703_hybrid_distractor_probe`.
- Sample/settings: `foodmenu6`, `minimax-m3`, `1RT`, distractor enabled.
- Raw rows: `hybridpatch=2`, `fullrewrite=2`; all rows have `distractor_included=True`.
- `verify_anchorpatch.py --dir anchorpatch/exp_20260703_hybrid_distractor_probe` PASS with `2` backward RS reproduced; `api_calls=4`, `api_anomalies=0`.

Val status:

- Out dir: `anchorpatch/exp_20260703_hybridval`.
- Samples: val20 from `analysis/hybrid_split.json`.
- Model/settings: `minimax-m3`, `10RT`, `--skip_distractor`, paired `hybridpatch` then `fullrewrite`, frozen theta `0.10`.
- Raw rows: `hybridpatch=400`, `fullrewrite=400`; run metadata has one frozen code fingerprint.
- `verify_anchorpatch.py --dir anchorpatch/exp_20260703_hybridval` PASS with `400` backward RS reproduced.
- Val metrics after replay: `hybridpatch` RS@10 `0.603`, `fullrewrite` RS@10 `0.474`; paired backward mean delta `+0.103` over `200` pairs; CriticalFailure@10 `hybridpatch=17/180`, `fullrewrite=18/180`.
- Hybrid telemetry: bounded rewrite share `283/400 (70.8%)`; kept-context failures `97/400`; forward no-effective-modification audit `14/200`; route violations include `distribution_missing_block=1`.

Test status:

- Out dir: `anchorpatch/exp_20260703_hybridtest`.
- Samples: test20 from `analysis/hybrid_split.json`.
- Model/settings: `minimax-m3`, `10RT`, distractor enabled, paired `hybridpatch` then `fullrewrite`, frozen theta `0.10`.
- Raw rows: `hybridpatch=400`, `fullrewrite=400`; all rows have `distractor_included=True`; run metadata has one frozen code fingerprint.
- M3 provider note: the second hybrid batch hit a 5-hour quota limit and was resumed after reset from runner checkpoints; raw/checkpoint/task/sample/evaluator files were not edited.
- `verify_anchorpatch.py --dir anchorpatch/exp_20260703_hybridtest` PASS with `400` backward RS reproduced from raw responses. A verifier-only distractor replay bug was fixed after the run by passing editable-only context into replay steps; this did not change experiment raw outputs, runner protocol, prompts, executor, or frozen run fingerprint.
- Test metrics after replay: `hybridpatch` RS@10 `0.584`, `fullrewrite` RS@10 `0.418`; paired backward mean delta `+0.181` over `200` pairs; CriticalFailure@10 `hybridpatch=17/180`, `fullrewrite=23/180`.
- Hybrid telemetry: bounded rewrite share `307/400 (76.8%)`; route share `local_patch=71/400 (17.8%)`, `bulk_patch=11/400 (2.8%)`, `dsl_rules=8/400 (2.0%)`, `hybridpatch_protocol_failure_kept_context=3/400 (0.8%)`; kept-context failures `70/400`; forward no-effective-modification audit `5/197`.
- Offline reporting fix: `analyze.py` was updated after test to label distractor status from result rows and to show `HybridPatch` as the primary method when no legacy `anchorpatch` rows exist. Metrics are unchanged.

## Open Dev Iteration: HybridPatch Improvement Round (post-test attribution)

The frozen val/test chain (`exp_20260703_hybridval`, `exp_20260703_hybridtest`) is complete and archived. Its code fingerprint is closed; any prompt/protocol/executor/schema change below is a NEW dev iteration and must not be reported as part of that frozen chain.

Attribution of the test win is archived in `anchorpatch/ANCHORPATCH_DESIGN_AND_FINDINGS.md` §213. Key findings driving this iteration:

- The win comes mainly from bounded_rewrite avoiding FR's catastrophic failure modes (max_tokens truncation dropping/renaming/wiping files; silent whole-doc content drift), plus the validation-gate + kept-context graceful degradation. Patch routes contribute near zero (bounded_rewrite share `76.8%`, dsl_rules 0% success).
- Self-inflicted bug: `malware3` scored 0 all 10 RT because the hybrid envelope embeds whole file bodies inside the JSON `action.files[].content` string; the model under-escaped backslashes one level, writing invalid `\G` to a YARA file. The gate did not catch it. This is the P0 target.
- Honesty risks to disclose in any report: no-op inflation (combined 50-sample flips negative under strict forward-no-op-zero audit; only distractor test stays positive), FR max_tokens dependence, val/test distractor confound, and reversed token cost (+61%).

Planned improvement order:

- P0 transport fix: move file bodies out of the nested-JSON `content`/`new_text` strings into a fenced raw-body section (reuse `utils_context` variable-length fences); envelope references bodies by sentinel. Add a backslash-dense byte-level round-trip fixture to `test_hybrid_executor.py`. This is the current active work.
- P1 attribution ablations: a scoped-FR-only arm (force bounded_rewrite) to measure patch-route contribution; an FR max_tokens cap-sensitivity check.
- P2 patch-route rework: fragment-id addressing to replace literal anchors (target the 72% anchor-not-found no-op); drop dsl_rules.
- P4 evidence hardening: second seed / second model, a no-distractor test arm or a distractor val arm to break the confound, `unused_reserve=20` as an improved-method sealed holdout, and forward-no-op-zero audited RS as a co-primary metric.

Guardrail for this iteration (unchanged from the track): honesty replay must reproduce every hybrid route from raw before any number is cited; no silent FR fallback; bounded-rewrite share reported every run; prompt/protocol iteration is dev-only and re-freezes before any new val/test.

## Previous Candidate State

No active candidate is currently open after C70 smoke closure.

The previous next eligible work was a C71 strategy card only. That path is superseded by the HybridPatch track above.

## Last Closed Candidate

C70 AP-pure filename-free visible span resolver smoke is closed and not promotable.

- Strategy card: `anchorpatch/runs/promptgen_20260626_minimax_m3_dev20_c70_filename_free_visible_span_resolver/analysis/strategy_card.md`.
- Smoke out_dir: `anchorpatch/runs/promptgen_20260626_minimax_m3_dev20_c70_filename_free_visible_span_resolver_smoke_eval`.
- No-API validation passed: `py_compile`, `test_executor.py` PASS=`236` FAIL=`0`, `splitters.py`, C70 AP dry-run, and C70 FR dry-run.
- AP+FR paired raw completed; honesty replay PASS; `analyze --K 2` and standard artifacts completed with `artifact_count=13`.
- C70 curve/final: `0.285987/0.185423`; paired FR curve/final: `0.866330/0.800055`; deltas `-0.580343/-0.614632`.
- Telemetry: actual AP-pure rows `7/20`, explicit rejects `13/20`, rows_committed `7`, rows_aborted `13`, zero-score collapses `7`, no-op forward count `3`, kept-context roundtrips `0`, fallback/emit/preservation violations `0`.
- Resolver signal: global exact matches `28`, global ambiguous matches `0`, global missing matches `25`, filename hints `0`.
- Provider/API attribution: cumulative `47` API calls all HTTP `200`; `14` anomaly rows are HTTP `200` method-content failures (`AP=13`, `FR=1`); retry/rate-limit/quota/transient/timeout counters `0`.
- Attribution: filename-free binding improved C69's source-binding hygiene but did not solve model span production or task reconstruction; this is not AP-pure capability evidence for promotion.
- Decision: not promotable; skip C70 devmini, devfull, sealed holdout, sealed materialization, and promotion flow.

## Recent Closed Candidates

- C69 before/after span diff compiler: smoke closed, not promotable; removed fallback/emit/kept-context inflation but collapsed through source-file/span binding and output-construction failures.
- C68 transaction repair patch compiler: smoke closed, not promotable; AP-pure transaction repair removed kept-context/fallback/emit-file inflation but collapsed through explicit rejects and weak selection.
- C67 semantic coverage delta transaction compiler: smoke closed, not promotable; preserved no-kept-context honesty and changed commits but underperformed FR.
- C66 commit-first delta program compiler: smoke closed, not promotable; produced real changed AP-pure commits but failed task-level reconstruction.
- C30-C65 are closed as not promotable or otherwise non-continuing in the current line; use `analysis/candidate_registry.md` for compact per-candidate evidence.

## Next Gate

Latest completed gate:

```text
After Code Change: HybridPatch/2 Executor-First Alignment + Thinking (no-API validation PASS)
```

Next executable gate:

```text
Before Experiment: MiniMax-M3 Thinking Probe, then HybridPatch/2 Dev Smoke Rerun
```

The frozen Phase 0-5 val/test chain is archived. Two dev fixes have landed and passed no-API regression: (1) P0 transport (fenced `[FILE BODIES]` + `@body:` sentinel, no nested-JSON escaping) — confirmed on the P0 smoke (`malware6` RS@10 `0.951` vs FR `0.892`, escape bug gone); (2) hybridpatch/2 executor-first alignment (FINDINGS §214) closing the executor-vs-prompt mismatch that caused 33% kept-context no-ops — E1 whitespace-tolerant matching, E2 file-level cross-block matching, E3 partial distribute, plus coarse-only block index, occurrence rule, and dsl de-promotion. All version-gated: `hybridpatch/1` archives replay byte-identically (honesty gate PASS on all four old dirs: 200/400/400/100 backward RS). `test_hybrid_executor.py` 14/14, `test_executor.py` 236, `splitters.py` byte-exact, `py_compile` green. MiniMax-M3 extended thinking is wired (`model_openai.py`, env `MINIMAX_THINKING`, no budget/max_tokens cap, temp forced to 1) but the API request shape is unverified. Next: one live thinking probe, then rerun the dev smoke (`malware6`/`latex2`/`mathlean2`/`foodmenu6`/`docker6`) with thinking on and high max_tokens to measure op_accept_rate/kept-context recovery. Do not tune on test20 samples (`malware3`, etc.).

## Forbidden Now

- Do not start C69 devmini, devfull, sealed holdout, sealed materialization, or promotion flow.
- Do not start C70 devmini, devfull, sealed holdout, sealed materialization, or promotion flow.
- Do not start C71 implementation, API/raw experiment, devmini, devfull, sealed holdout, sealed materialization, or promotion flow; C71 is superseded by the hybridpatch track.
- Do not report any `hybridpatch` result before `verify_anchorpatch.py` can replay it from raw responses.
- Do not treat `hybridpatch` bounded rewrite rows as AP-pure evidence. They are allowed only as disclosed HybridPatch route share.
- Do not start C66, C67, C68, or earlier devmini, devfull, sealed holdout, sealed materialization, or promotion flow.
- Do not edit raw result JSONL, checkpoints, frozen task plans, samples/references, evaluator code, or scoring semantics.
- Do not use router/fallback-primary candidates, late repair prompts, rejection-only gates, FR/scoped-FR rows, kept-context/no-op rows, FR collapse, reference content, target content, evaluator scores, hidden diffs, or sealed signals as AP-pure capability evidence.
